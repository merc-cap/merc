import { Card, Stack, Typography } from "@mui/material";

const GaugeList = () => {
  return (
    <Stack>
      <Card sx={{ p: 4 }}>
        <Typography variant="h3">MERC-yvCurve-GUSD</Typography>
      </Card>
    </Stack>
  );
};

export default GaugeList;
